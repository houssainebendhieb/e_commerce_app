import 'package:flutter/material.dart';

class HomePage extends StatelessWidget {
  static String id = "HomePage";
  Widget build(context) {
    return Text('hello world you are in home page ');
  }
}
